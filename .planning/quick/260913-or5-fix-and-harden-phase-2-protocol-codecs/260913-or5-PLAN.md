---
quick_id: 260913-or5
mode: quick-full
title: Fix and harden phase 2 protocol codecs
must_haves:
  truths:
    - Feature packets preserve the established 64-byte layout and validate headers before sending.
    - Only packet-derived allowed opcodes and matching write modes reach transport.
    - LCD frames transmit eight raw 4096-byte chunks without synthetic headers.
    - Invalid chunk counts and short writes fail explicitly without continuing.
  artifacts:
    - crates/monkey-core/src/protocol/types.rs
    - crates/monkey-core/src/protocol/safety.rs
    - crates/monkey-core/tests/protocol_codecs_test.rs
    - crates/monkey-core/tests/transaction_safety_test.rs
  key_links:
    - HardwareChannel dispatches the same validated TransactionManager send path.
    - Benchmark runners use canonical packet types and F5 state readback.
---

# Approved implementation plan

Source: user-approved plan in this task. Baseline HEAD a0d78e9; working tree clean.
The benchmark was committed after review (e1fe175); preserve its existing CLI registration and behavior, adapting only protocol-dependent code and measurement labels where needed.
Run inline per the gsd-quick Codex spawn restriction. No hardware I/O, no dependency changes, no full phase completion, no lifecycle/RAII/reset expansion.

## Task 1: Canonical packets and safe send path
- files: protocol/{types,codecs,framing,safety,transaction,channel,mod}.rs, src/lib.rs, src/bench/mod.rs
- action: Remove competing packet layouts; add bounded payload constructor and exact header parser; use raw BulkChunkPacket. Compute chunks in usize and reject >255 before narrowing, reject zero/oversize sizes, keep sequence metadata outside wire bytes and pad terminal chunks. Derive opcode from packet, allow only 18/13/20/02/F0/F5, bind flash mode to 02, retain debounce, abort on short write. Migrate benchmark to F5 and canonical chunks.
- verify: Regression tests from task 2 plus cargo check --workspace --all-targets.
- done: No legacy layout callers remain; invalid inputs never reach transport; no speculative wire headers.

## Task 2: Regression coverage and compatibility
- files: crates/monkey-core/tests/{protocol_codecs_test,transaction_safety_test}.rs, crates/monkey-cli/tests/cli_bench_test.rs
- action: Independent byte fixtures; invalid header/length/payload; exhaustive 256 opcodes and both modes through manager and worker; mutated packets; raw eight-chunk frame equality; 0/4096/4097 chunk sizes, empty/255/256 counts, final padding; errors/short writes stop progress. Adapt existing benchmark tests.
- verify: cargo test --workspace; cargo check --workspace --all-targets; cargo clippy --workspace --all-targets -- -D warnings; formatting check of changed Rust files.
- done: Tests/build pass; distinguish baseline lint from introduced errors. Baseline strict clippy fails on two manual_div_ceil expressions in framing.rs, which this task replaces.

## Task 3: Verification and GSD record
- files: this directory's SUMMARY.md and VERIFICATION.md, .planning/STATE.md
- action: Verify each must-have against tests/source; record evidence source and no-hardware limitation; update quick task status only. Commit code and docs separately.
- verify: git diff --check and inspect staged file scope.
- done: Quick task Verified, Phase 2 remains awaiting verification.

## Plan check (inline)
PASS: approved requirements covered by three bounded tasks; files exist; testable must-haves and cross-module links specified. Protocol authority is Phase 2 CONTEXT allowlist and existing FeatureReportPacket/BulkChunkPacket layout, not a claim of physical Monka verification. No new library APIs or hardware experiments required.
