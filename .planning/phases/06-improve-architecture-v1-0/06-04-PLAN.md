---
phase: 06-improve-architecture-v1-0
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - docs/dev/architecture.md
  - Cargo.toml
  - crates/monkey-core/Cargo.toml
  - crates/monkey-cli/Cargo.toml
autonomous: true
requirements:
  - ARCH-01

estimate:
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Workspace test suite passes with 122+ unit, integration, and CLI tests green and 0 regressions per D-06"
    - "Workspace code adheres to strict zero-warning clippy standard (-D warnings) per D-06"
    - "Cargo-deny license audit and vulnerability check pass with 0 errors or warnings per D-06"
    - "docs/dev/architecture.md is updated with the target MonkaDevice lifecycle, InterfacePolicy, and SafeTransport guard façade contracts"
    - "GitNexus code intelligence graph is refreshed to reflect the consolidated MonkaDevice call graph per D-06"
  artifacts:
    - docs/dev/architecture.md
  key_links:
    - "docs/dev/architecture.md describes the layered MonkaDevice and SafeTransport architecture"
    - "GitNexus code graph indexes MonkaDevice and SafeTransport symbols"
---

# Phase 6 Plan 04: Regression Verification, Documentation, and GitNexus Re-indexing (Wave 4)

**As a** developer and maintainer, **I want to** run complete workspace regression verification, eliminate all compiler and clippy warnings, update architecture documentation, and re-index the GitNexus code graph, **so that** MonKey v1.0 architecture improvements are verified, documented, and traceable.

<objective>
Execute workspace-wide test regression verification ensuring all 122+ unit, integration, and CLI tests pass without failure. Enforce zero compiler warnings and zero Clippy lints (`-D warnings`). Update `docs/dev/architecture.md` with complete specifications of `MonkaDevice`, `InterfacePolicy`, `SafeTransport`, and `OpenError`. Re-index the GitNexus code graph and verify that the critical blast radius of raw HID endpoint callers has collapsed into the centralized `MonkaDevice` coordinator.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-improve-architecture-v1-0/06-CONTEXT.md
@.planning/phases/06-improve-architecture-v1-0/06-RESEARCH.md
@.planning/phases/06-improve-architecture-v1-0/06-PATTERNS.md
@.planning/phases/06-improve-architecture-v1-0/06-VALIDATION.md
@.planning/phases/06-improve-architecture-v1-0/06-03-PLAN.md
@docs/dev/architecture.md
</context>

<tasks>

<task type="tracer" id="06-04-01">
  <name>Task 1: Execute workspace-wide regression suite, lint check, and license audit tracer slice</name>
  <files>Cargo.toml, crates/monkey-core/Cargo.toml, crates/monkey-cli/Cargo.toml</files>
  <action>
    Execute workspace-wide quality and regression verification per decision D-06:
    - Run `cargo test --all-targets` across the entire workspace. Verify all 122+ unit, integration, and CLI tests pass cleanly without errors.
    - Run `cargo clippy --all-targets -- -D warnings` enforcing strict static analysis, verifying zero dead code, redundant clones, or lint warnings across all crates.
    - Run `cargo deny check` ensuring license compliance and zero security advisories.
  </action>
  <verify>
    <automated>cargo test --all-targets && cargo clippy --all-targets -- -D warnings && cargo deny check</automated>
    <fails_when>non-zero exit code, test failure, clippy lint warning, or cargo-deny audit failure</fails_when>
  </verify>
  <done>
    Full workspace test suite passes with 122+ tests green, zero clippy warnings, and zero cargo-deny audit violations per D-06.
  </done>
</task>

<task type="auto" id="06-04-02">
  <name>Task 2: Update architecture documentation and GitNexus code graph</name>
  <files>docs/dev/architecture.md</files>
  <action>
    Update architecture specifications and refresh GitNexus code intelligence per decisions D-01 through D-07:
    - In `docs/dev/architecture.md`:
      - Update component diagrams to reflect the deep-module `MonkaDevice` hierarchy and `SafeTransport<'a>` guard façade.
      - Document `MonkaDevice` as the primary hardware coordinator and the single test seam principle (`MonkaDevice::from_transport`) per D-01.
      - Document `InterfacePolicy` enum (`PreferB`, `RequireA`, `RequireB`, `BulkFirst`, `ControlFirst`, `Any`) and interface variation semantics per D-02.
      - Document `OpenError` variants and non-fail-fast `MonkaDevice::diagnose()` per D-03.
      - Document internal `SafetyRails` ownership and deep module operations per D-04.
      - Document the pure `Transport` trait and `SafeTransport<'a>` write authorization gate per D-07.
      - Note the eradication of `MONKEY_SIMULATE_EMPTY` and unification of test seams per D-05.
    - Re-index GitNexus:
      - Run `node .gitnexus/run.cjs analyze --index-only` from project root to refresh the AST and call graph.
      - Verify that callers of `open_device_path` and `find_monka_device_sets` have consolidated to `MonkaDevice` and `doctor`, reducing direct upstream blast radius.
  </action>
  <verify>
    <automated>cargo test --all-targets</automated>
    <fails_when>non-zero exit code or compilation failure after documentation updates</fails_when>
  </verify>
  <done>
    `docs/dev/architecture.md` accurately documents the target architecture and GitNexus code intelligence is fully refreshed per D-06.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → Developer Assumptions | Accuracy of architecture documents describing hardware safety invariants |
| Dependency Tree → Workspace Binaries | Third-party crates and vulnerability databases verified via cargo-deny |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering / Supply Chain | Workspace dependencies | High | mitigate | `cargo-deny` audit checks all licenses and security advisories, failing build on any vulnerability or unapproved license. |
| T-06-08 | Repudiation / Inconsistent State | `docs/dev/architecture.md` | Low | mitigate | Keep documentation strictly aligned with concrete implementation types (`MonkaDevice`, `InterfacePolicy`, `SafeTransport`). |
</threat_model>

<verification>
Run complete workspace validation:
```bash
cargo test --all-targets && cargo clippy --all-targets -- -D warnings && cargo deny check
```
Verify 100% clean test passes, 0 clippy warnings, and clean audits.
</verification>

<success_criteria>
1. All 122+ workspace tests pass with zero regressions (D-06).
2. `cargo clippy --all-targets -- -D warnings` completes with zero warnings (D-06).
3. `cargo deny check` completes with zero errors or warnings (D-06).
4. `docs/dev/architecture.md` documents all decisions D-01 through D-07.
5. GitNexus code graph is refreshed and validated.
</success_criteria>
