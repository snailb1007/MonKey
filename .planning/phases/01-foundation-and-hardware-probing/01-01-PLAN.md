---
phase: 01-foundation-and-hardware-probing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/monkey-core/Cargo.toml
  - crates/monkey-core/src/lib.rs
  - crates/monkey-core/src/error.rs
  - crates/monkey-core/src/transport/mod.rs
  - crates/monkey-core/src/transport/mock.rs
  - crates/monkey-core/src/protocol/mod.rs
  - crates/monkey-core/src/protocol/types.rs
  - crates/monkey-core/tests/mock_transport_test.rs
  - crates/monkey-core/tests/protocol_types_test.rs
autonomous: true
requirements:
  - DISC-04

estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Cargo virtual workspace compiles crates/monkey-core cleanly without errors"
    - "Transport trait defines synchronous Send methods for write_bulk, send_feature_report, get_feature_report, and read_input_report per D-04"
    - "MockTransport accurately records calls, serves canned responses, and provides assert_no_writes for headless testing per D-06 and D-12"
    - "Zero-copy packet structs derive zerocopy traits with explicit little-endian byteorder primitives per D-07, D-08, and D-10"
  artifacts:
    - Cargo.toml
    - crates/monkey-core/Cargo.toml
    - crates/monkey-core/src/lib.rs
    - crates/monkey-core/src/error.rs
    - crates/monkey-core/src/transport/mod.rs
    - crates/monkey-core/src/transport/mock.rs
    - crates/monkey-core/src/protocol/mod.rs
    - crates/monkey-core/src/protocol/types.rs
    - crates/monkey-core/tests/mock_transport_test.rs
    - crates/monkey-core/tests/protocol_types_test.rs
  key_links:
    - "MockTransport in crates/monkey-core/src/transport/mock.rs implements Transport from crates/monkey-core/src/transport/mod.rs"
    - "Protocol packet structures in crates/monkey-core/src/protocol/types.rs enforce fixed 64-byte and 4096-byte compile-time size invariants"
---

# Phase 1 Plan 01: Cargo Virtual Workspace, Transport Trait, and MockTransport

**As a** keyboard user or developer, **I want to** discover and probe my Monka 3075 Pro over USB HID, **so that** I can verify device identity and connection health without triggering OS security prompts.

<objective>
Establish the virtual Cargo workspace root and foundational driver library `crates/monkey-core`. Define the synchronous `Transport` trait, structured `TransportError`, deterministic in-memory `MockTransport` with call recording, and zero-copy packet layout definitions with compile-time size assertions.

Purpose: Provide a modular, headless-testable transport seam and zero-copy memory foundation that subsequent hardware discovery and CLI layers build upon without depending on physical hardware access in CI.
Output: Valid Cargo workspace containing `crates/monkey-core`, `Transport` trait, `MockTransport`, zero-copy packet layouts, and automated unit test suite.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-hardware-probing/01-CONTEXT.md
@.planning/phases/01-foundation-and-hardware-probing/RESEARCH.md
@research/device_info.json
@research/prior_art_protocol.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Initialize Cargo virtual workspace, Transport trait seam, and TransportError</name>
  <files>Cargo.toml, crates/monkey-core/Cargo.toml, crates/monkey-core/src/lib.rs, crates/monkey-core/src/error.rs, crates/monkey-core/src/transport/mod.rs</files>
  <action>
    Create the root virtual Cargo.toml declaring workspace members `crates/monkey-core` and `crates/monkey-cli` with `resolver = "2"`. Declare centralized workspace dependencies with exact versions vetted in the Package Legitimacy Audit: `hidapi = { version = "2.6.7", features = ["macos-shared-device"] }` per D-03, `zerocopy = { version = "0.8.57", features = ["derive"] }`, `thiserror = "2.0.20"`, `tracing = "0.1.44"`, and `serde = { version = "1.0.229", features = ["derive"] }`.

    Create `crates/monkey-core/Cargo.toml` inheriting workspace dependencies (`workspace = true`). In `crates/monkey-core/src/error.rs`, implement `TransportError` using `thiserror::Error` with variants: `DeviceNotFound(String)`, `HidError(String)`, `IoError(String)`, `Timeout`, `BufferTooSmall { needed: usize, provided: usize }`, `InvalidReportId(u8)`, `Disconnected`, and `ProtocolViolation(String)`.

    In `crates/monkey-core/src/transport/mod.rs`, define the synchronous `Transport: Send` trait per D-04 with four core methods: `write_bulk(&mut self, report_id: u8, data: &[u8]) -> Result<usize, TransportError>`, `send_feature_report(&mut self, data: &[u8]) -> Result<(), TransportError>`, `get_feature_report(&mut self, report_id: u8, buf: &mut [u8]) -> Result<usize, TransportError>`, and `read_input_report(&mut self, buf: &mut [u8], timeout_ms: i32) -> Result<usize, TransportError>`. Export `Transport` and `TransportError` from `crates/monkey-core/src/lib.rs`.
  </action>
  <verify>
    <automated>cargo check -p monkey-core</automated>
    <fails_when>Cargo workspace fails to resolve dependencies or monkey-core compilation fails</fails_when>
  </verify>
  <done>
    Virtual Cargo workspace resolves correctly and monkey-core compiles cleanly with the Transport trait and TransportError defined.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic MockTransport with call recording and assertion helpers</name>
  <files>crates/monkey-core/src/transport/mock.rs, crates/monkey-core/src/transport/mod.rs, crates/monkey-core/tests/mock_transport_test.rs</files>
  <behavior>
    - Test 1: MockTransport records write_bulk, send_feature_report, get_feature_report, and read_input_report calls in FIFO sequence with report IDs, timeouts, and owned payload copies.
    - Test 2: MockTransport returns pre-seeded canned feature reports by report ID, returning BufferTooSmall when the destination buffer is shorter than the canned payload.
    - Test 3: MockTransport drains pre-queued input reports in FIFO order or returns TransportError::Timeout when the input queue is empty.
    - Test 4: MockTransport::assert_no_writes() passes when no write_bulk or send_feature_report calls were invoked, and returns an assertion error if any write occurred, enforcing read-only probing safety per D-12.
    - Test 5: Injected errors (e.g. TransportError::Disconnected) fail the corresponding operation immediately without side effects.
  </behavior>
  <action>
    Create `crates/monkey-core/src/transport/mock.rs` implementing `Transport` per D-04 and D-06. Define `TransportCall` enum capturing operation variants (`WriteBulk { report_id: u8, data: Vec<u8> }`, `SendFeature { data: Vec<u8> }`, `GetFeature { report_id: u8 }`, `ReadInput { timeout_ms: i32 }`).

    Inside `MockTransport`, maintain an in-memory `calls: Vec<TransportCall>`, a `feature_responses: HashMap<u8, Vec<u8>>` for canned feature responses, a `input_queue: VecDeque<Vec<u8>>` for input reports, and an optional `injected_error: Option<TransportError>`. Implement builder/mutator methods `set_feature_response(report_id, bytes)`, `queue_input_report(bytes)`, `inject_error(error)`, `calls() -> &[TransportCall]`, `clear_calls()`, and `assert_no_writes(&self) -> Result<(), String>`.

    Expose `MockTransport` under `monkey_core::transport::MockTransport`. Write comprehensive unit tests in `crates/monkey-core/tests/mock_transport_test.rs` validating all recorded call variants, canned responses, queue drains, and write assertion safety guards.
  </action>
  <verify>
    <automated>cargo test -p monkey-core --test mock_transport_test</automated>
    <fails_when>Unit tests fail, call recording misses operations, canned responses mismatch, or assert_no_writes misbehaves</fails_when>
  </verify>
  <done>
    MockTransport provides fully deterministic in-memory recording and playback with zero hardware or thread dependencies, fulfilling DISC-04 and D-06.
  </done>
</task>

<task type="auto">
  <name>Task 3: Define zero-copy packet layouts with little-endian primitives and compile-time size guards</name>
  <files>crates/monkey-core/src/protocol/mod.rs, crates/monkey-core/src/protocol/types.rs, crates/monkey-core/src/lib.rs, crates/monkey-core/tests/protocol_types_test.rs</files>
  <action>
    Create `crates/monkey-core/src/protocol/types.rs` defining fixed-size hardware packet layouts per D-07, D-08, and D-10. Derive `zerocopy::FromBytes`, `zerocopy::IntoBytes`, `zerocopy::Immutable`, and `zerocopy::KnownLayout` on all protocol structures.

    Use explicit little-endian integer types `zerocopy::byteorder::little_endian::{U16, U32}` per D-08 to ensure deterministic byte ordering across Apple Silicon (ARM64) and x86_64 architectures.

    Define `FeatureReportPacket` with `#[repr(C, packed)]` representing fixed 64-byte feature reports (D-10): `magic: u8` (expected `0x04`), `command: u8`, `args: [u8; 6]`, `chunk_index: u8`, `reserved: [u8; 5]`, `marker: [u8; 2]` (expected `0xAA, 0x55`), and `payload: [u8; 48]`. Add helper methods `validate_header(&self) -> Result<(), TransportError>` per D-09 ensuring magic is `0x04` and markers at offset 14-15 match `0xAA, 0x55`.

    Define `BulkChunkPacket` with `#[repr(C, packed)]` representing 4096-byte bulk chunk buffers (D-10) for LCD data frames.

    Add compile-time size verification assertions using `const _: () = assert!(core::mem::size_of::<FeatureReportPacket>() == 64);` and `const _: () = assert!(core::mem::size_of::<BulkChunkPacket>() == 4096);`.

    Export protocol types in `crates/monkey-core/src/lib.rs`. Write unit tests in `crates/monkey-core/tests/protocol_types_test.rs` asserting zero-allocation transmute from byte slices and round-trip byte conversions.
  </action>
  <verify>
    <automated>cargo test -p monkey-core --test protocol_types_test</automated>
    <fails_when>Compile-time size assertion fails, zerocopy derives fail, or packet parsing produces framing errors</fails_when>
  </verify>
  <done>
    Zero-copy packet structs are verified with compile-time 64-byte and 4096-byte size guarantees and little-endian layout safety, fulfilling D-07, D-08, D-09, and D-10.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Crates.io Registry → Cargo Build | External crate download crossing development boundary into workspace build |
| Unsafe Byte Slices → Zerocopy Transmutes | Raw hardware buffers cast into typed Rust structures |
| Mock In-Memory State → Test Assertions | Synthetic test inputs driving driver state machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `crates/monkey-core/src/protocol/types.rs` | High | mitigate | Enforce `zerocopy` derived traits (`FromBytes`, `IntoBytes`, `Immutable`, `KnownLayout`) with `#[repr(C, packed)]` and compile-time size assertions `size_of == 64` and `size_of == 4096` to prevent alignment traps and buffer overruns |
| T-01-02 | Information Disclosure | `crates/monkey-core/src/protocol/types.rs` | Medium | mitigate | Zero-fill all padding buffers in `FeatureReportPacket` and `BulkChunkPacket` before serialization to prevent uninitialized memory leakage |
| T-01-SC | Tampering | Cargo workspace dependencies | High | mitigate | Restrict dependencies to audited, reputable packages from crates.io (`hidapi 2.6.7`, `zerocopy 0.8.57`, `thiserror 2.0.20`) with strict version pinning in `Cargo.lock` per Package Legitimacy Audit |
</threat_model>

<verification>
Execute the full monkey-core unit test suite:
```bash
cargo test -p monkey-core
```
Ensure all mock transport recording, canned responses, zero-copy packet layout verifications, and size assertions pass with zero failures or compiler warnings.
</verification>

<success_criteria>
1. Root virtual Cargo workspace compiles cleanly with `cargo check -p monkey-core`.
2. `Transport` synchronous trait defines `write_bulk`, `send_feature_report`, `get_feature_report`, and `read_input_report`.
3. `MockTransport` provides deterministic in-memory recording, canned responses, and passes all tests in `mock_transport_test.rs`.
4. `FeatureReportPacket` (64 bytes) and `BulkChunkPacket` (4096 bytes) derive `zerocopy` traits and validate compile-time size invariants.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-hardware-probing/01-01-SUMMARY.md` when done
</output>
